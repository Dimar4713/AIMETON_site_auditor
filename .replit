entrypoint = "app/main.py"
run = "uvicorn app.main:app --host 0.0.0.0 --port 5000"

[deployment]
run = ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-5000}"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 5000
externalPort = 80
