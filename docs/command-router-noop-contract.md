# Unknown command no-op contract

Unsupported or malformed slash commands are recorded as ignored by the router and must not create a downstream workflow dispatch. They do not change claims, permissions, policy, secrets, budget or execution state.
