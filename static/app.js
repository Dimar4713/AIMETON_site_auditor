let analysis=null;const f=document.querySelector('#form'),status=document.querySelector('#status'),result=document.querySelector('#result'),chat=document.querySelector('#chat');
f.onsubmit=async(e)=>{e.preventDefault();status.textContent='Исследуем экономические сигналы…';result.classList.add('hidden');chat.classList.add('hidden');try{const r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:document.querySelector('#url').value})});if(!r.ok)throw new Error((await r.json()).detail||'Ошибка');analysis=await r.json();render();status.textContent='Коммерческая возможность подготовлена';}catch(err){status.textContent='Ошибка: '+err.message}};
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function render(){const o=analysis.commercial_opportunity,p=analysis.action_package;result.innerHTML=`<h2>${esc(analysis.company_name)}</h2><p>${esc(analysis.business_summary)}</p><section class="panel"><h3>Коммерческая возможность</h3><p><span class="tag">${esc(o.qualification)} · ${esc(o.score)}/100</span></p><h3>${esc(o.opportunity_type)}</h3><p><strong>Гипотеза проблемы</strong><br>${esc(o.problem_hypothesis)}</p><p><strong>Рекомендуемое решение</strong><br>${esc(o.recommended_solution)}</p><p><strong>Ожидаемая ценность</strong><br>${esc(o.expected_value)}</p></section><h3>Экономические сигналы</h3><div class="grid">${analysis.economic_signals.map(s=>`<article class="card"><span class="tag">Уверенность: ${esc(s.confidence)}</span><h3>${esc(s.signal)}</h3><p><strong>Основание</strong><br>${esc(s.evidence)}</p><p><strong>Возможный эффект</strong><br>${esc(s.business_effect)}</p></article>`).join('')}</div><h3>Подходящие AI-инструменты</h3><div class="grid">${analysis.agents.map(a=>`<article class="card"><span class="tag">${esc(a.priority)}</span><h3>${esc(a.name)}</h3><p>${esc(a.purpose)}</p><strong>Польза</strong><p>${esc(a.benefit)}</p></article>`).join('')}</div><section class="panel"><h3>Пакет действия</h3><p><strong>Предполагаемый ЛПР</strong><br>${esc(p.decision_maker_hypothesis)}</p><p><strong>Основание для контакта</strong><br>${esc(p.contact_reason)}</p><p><strong>Демонстрационный сценарий</strong></p><ol>${p.demo_scenario.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><p><strong>Первое сообщение</strong></p><blockquote>${esc(p.first_message)}</blockquote><p><strong>Следующий шаг</strong><br>${esc(p.next_action)}</p></section><h3>Ограничения и предположения</h3><ul>${analysis.risks_and_assumptions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;result.classList.remove('hidden');chat.classList.remove('hidden')}
document.querySelector('#chatForm').onsubmit=async(e)=>{e.preventDefault();const q=document.querySelector('#question'),text=q.value;q.value='';add(text,'user');const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({analysis,messages:[{role:'user',content:text}]})});const data=await r.json();add(data.reply,'assistant')};function add(t,c){const d=document.createElement('div');d.className='msg '+c;d.textContent=t;document.querySelector('#messages').append(d)}
