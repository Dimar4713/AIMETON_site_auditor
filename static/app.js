let analysis=null;const f=document.querySelector('#form'),status=document.querySelector('#status'),result=document.querySelector('#result'),chat=document.querySelector('#chat');
f.onsubmit=async(e)=>{e.preventDefault();status.textContent='Анализируем сайт…';result.classList.add('hidden');chat.classList.add('hidden');try{const r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:document.querySelector('#url').value})});if(!r.ok)throw new Error((await r.json()).detail||'Ошибка');analysis=await r.json();render();status.textContent='Анализ завершён';}catch(err){status.textContent='Ошибка: '+err.message}};
function render(){result.innerHTML=`<h2>${analysis.company_name}</h2><p>${analysis.business_summary}</p><h3>Рекомендуемые AI-агенты</h3><div class="grid">${analysis.agents.map(a=>`<article class="card"><span class="tag">${a.priority}</span><h3>${a.name}</h3><p>${a.purpose}</p><strong>Польза</strong><p>${a.benefit}</p></article>`).join('')}</div><h3>Ограничения анализа</h3><ul>${analysis.risks_and_assumptions.map(x=>`<li>${x}</li>`).join('')}</ul>`;result.classList.remove('hidden');chat.classList.remove('hidden')}
document.querySelector('#chatForm').onsubmit=async(e)=>{e.preventDefault();const q=document.querySelector('#question'),text=q.value;q.value='';add(text,'user');const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({analysis,messages:[{role:'user',content:text}]})});const data=await r.json();add(data.reply,'assistant')};function add(t,c){const d=document.createElement('div');d.className='msg '+c;d.textContent=t;document.querySelector('#messages').append(d)}
