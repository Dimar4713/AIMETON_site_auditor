name: VERIFIER-P0 RouterAI Capability Probe

on:
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Exact commit SHA containing the admitted probe code and model registry
        required: true
        type: string
      allow_paid_calls:
        description: Explicit permission to perform two bounded provider calls
        required: true
        default: false
        type: boolean
      owner_spend_authorized:
        description: Owner explicitly authorized bounded spend
        required: true
        default: false
        type: boolean
      max_budget_rub:
        description: Maximum owner-authorized budget in RUB
        required: true
        default: "100"
        type: string
      evidence_issue:
        description: Issue that receives sanitized terminal evidence
        required: true
        default: "783"
        type: string
      profile:
        description: Allowlisted verifier profile id from config/verifier_model_profiles.json
        required: true
        default: "routerai-gpt4o-mini"
        type: string

permissions:
  contents: read
  issues: write

concurrency:
  group: verifier-routerai-capability-probe-${{ inputs.profile }}
  cancel-in-progress: false

jobs:
  probe:
    runs-on: [self-hosted, Linux, X64, stage, auditor]
    environment: stage
    timeout-minutes: 10
    env:
      TARGET_SHA: ${{ inputs.expected_sha }}
      ROUTERAI_API_KEY: ${{ secrets.ROUTERAI_API_KEY }}
      VERIFIER_PROFILE: ${{ inputs.profile }}
      VERIFIER_MAX_BUDGET_RUB: ${{ inputs.max_budget_rub }}
      VERIFIER_RESULT_PATH: /tmp/aimeton-verifier-routerai-capability-${{ github.run_id }}.json
      VENV_DIR: /tmp/aimeton-verifier-probe-${{ github.run_id }}
      GH_TOKEN: ${{ github.token }}
      EVIDENCE_ISSUE: ${{ inputs.evidence_issue }}

    steps:
      - name: Require exact SHA and explicit owner spend authorization
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$TARGET_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ '${{ inputs.allow_paid_calls }}' == 'true' ]]
          [[ '${{ inputs.owner_spend_authorized }}' == 'true' ]]
          [[ "$EVIDENCE_ISSUE" == '783' ]]
          [[ "$VERIFIER_PROFILE" =~ ^[a-z0-9-]+$ ]]
          [[ -n "${ROUTERAI_API_KEY:-}" ]]
          python3 - <<'PY'
          import os
          budget = float(os.environ['VERIFIER_MAX_BUDGET_RUB'])
          assert 0 < budget <= 100.0, budget
          PY

      - name: Materialize exact commit without marketplace actions
        shell: bash
        run: |
          set -Eeuo pipefail
          cd "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          if [[ ! -d .git ]]; then git init; fi
          git remote remove origin 2>/dev/null || true
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          git fetch --no-tags --depth=1 origin "$TARGET_SHA"
          git checkout --detach --force FETCH_HEAD
          git reset --hard "$TARGET_SHA"
          git clean -ffdx
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test -f scripts/verifier_routerai_capability_probe.py
          test -f app/verifier_backend_capability.py
          test -f app/verifier_model_profiles.py
          test -f config/verifier_model_profiles.json

      - name: Create minimal isolated probe environment
        shell: bash
        run: |
          set -Eeuo pipefail
          rm -rf "$VENV_DIR"
          python3 -m venv "$VENV_DIR"
          "$VENV_DIR/bin/python" -m pip install --upgrade pip
          "$VENV_DIR/bin/pip" install 'pydantic>=2.11,<3'

      - name: Execute bounded unconstrained and structured RouterAI probes
        shell: bash
        run: |
          set +e
          cd "$GITHUB_WORKSPACE"
          PYTHONPATH="$GITHUB_WORKSPACE" "$VENV_DIR/bin/python" scripts/verifier_routerai_capability_probe.py
          rc=$?
          echo "PROBE_RC=$rc" >> "$GITHUB_ENV"
          exit 0

      - name: Publish sanitized capability evidence to issue 783
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 - <<'PY'
          import json
          import os
          import urllib.request
          from pathlib import Path

          path = Path(os.environ['VERIFIER_RESULT_PATH'])
          if path.exists():
              payload = json.loads(path.read_text(encoding='utf-8'))
          else:
              payload = {
                  'schema_version': '1.2',
                  'profile_id': os.environ['VERIFIER_PROFILE'],
                  'backend_id': 'routerai',
                  'qualification_status': 'probe_execution_failure',
                  'reason_codes': ['sanitized_result_missing'],
                  'provider_calls': 0,
                  'max_budget_rub': float(os.environ['VERIFIER_MAX_BUDGET_RUB']),
                  'raw_response_saved': False,
                  'client_release_authority': False,
                  'hard_gate_override': False,
              }

          compact = json.dumps(payload, ensure_ascii=False, sort_keys=True, indent=2)
          body = (
              '## VERIFIER-P0 RouterAI score-distribution capability evidence\n\n'
              f'- workflow run: `{os.environ["GITHUB_RUN_ID"]}`\n'
              f'- exact SHA / registry revision: `{os.environ["TARGET_SHA"]}`\n'
              f'- verifier profile: `{os.environ["VERIFIER_PROFILE"]}`\n'
              f'- resolved model: `{payload.get("requested_model")}`\n'
              f'- owner-authorized ceiling: `{os.environ["VERIFIER_MAX_BUDGET_RUB"]} RUB`\n'
              '- provider calls admitted: `2` (unconstrained + strict json_schema)\n'
              '- raw provider responses retained: `no`\n'
              '- client release / hard-gate authority: `none`\n\n'
              '```json\n' + compact + '\n```\n'
          )
          repo = os.environ['GITHUB_REPOSITORY']
          issue = os.environ['EVIDENCE_ISSUE']
          req = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/issues/{issue}/comments',
              data=json.dumps({'body': body}, ensure_ascii=False).encode('utf-8'),
              method='POST',
              headers={
                  'Authorization': f'Bearer {os.environ["GH_TOKEN"]}',
                  'Accept': 'application/vnd.github+json',
                  'Content-Type': 'application/json',
                  'X-GitHub-Api-Version': '2022-11-28',
              },
          )
          with urllib.request.urlopen(req, timeout=30) as response:
              assert response.status == 201, response.status

          summary = os.environ.get('GITHUB_STEP_SUMMARY')
          if summary:
              modes = payload.get('probe_modes') or {}
              unconstrained = modes.get('unconstrained_top_logprobs') or {}
              structured = modes.get('structured_json_schema') or {}
              with open(summary, 'a', encoding='utf-8') as handle:
                  handle.write('## VERIFIER-P0 RouterAI score-distribution capability\n\n')
                  handle.write(f'- profile: `{payload.get("profile_id")}`\n')
                  handle.write(f'- model: `{payload.get("requested_model")}`\n')
                  handle.write(f'- qualification: `{payload.get("qualification_status")}`\n')
                  handle.write(f'- unconstrained support: `{unconstrained.get("max_score_support")}`\n')
                  handle.write(f'- structured support: `{structured.get("max_score_support")}`\n')
                  handle.write(f'- estimated cost RUB: `{payload.get("actual_estimated_cost_rub")}`\n')
                  handle.write('- raw response retained: `no`\n')
          PY

      - name: Enforce structured score-distribution qualification gate
        shell: bash
        run: |
          set -Eeuo pipefail
          rc="${PROBE_RC:-99}"
          [[ "$rc" =~ ^[0-9]+$ ]]
          if [[ "$rc" != 0 ]]; then
            echo "RouterAI structured score-distribution path did not qualify; see sanitized evidence in #783" >&2
            exit "$rc"
          fi

      - name: Cleanup isolated probe environment
        if: always()
        shell: bash
        run: |
          rm -rf "$VENV_DIR"
          rm -f "$VERIFIER_RESULT_PATH"
