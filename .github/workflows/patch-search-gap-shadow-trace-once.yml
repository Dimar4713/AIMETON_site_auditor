name: Patch Search Gap Shadow Trace Once

"on":
  push:
    branches:
      - feat/search-gap-shadow-trace-632

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python scripts/_patch_search_gap_shadow_trace_once.py
      - name: Commit patch
        run: |
          git config user.name "AIMETON Agent"
          git config user.email "127470701+Dimar4713@users.noreply.github.com"
          git add app/models.py app/discovery.py app/main.py
          git diff --cached --check
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "feat: retain gap-driven shadow suggestion identity"
          git push origin HEAD:feat/search-gap-shadow-trace-632
