name: Temporary Authorized Hunter Real E2E Stage

on:
  push:
    branches:
      - chore/hunter-real-e2e-router

permissions:
  contents: read
  issues: write

concurrency:
  group: tmp-authorized-hunter-real-e2e-stage
  cancel-in-progress: false

jobs:
  accept:
    runs-on: [self-hosted, Linux, X64, stage, auditor]
    environment: stage
    timeout-minutes: 35
    env:
      STACK_DIR: /opt/aimeton/auditor-stack
      CONTAINER: aimeton-auditor
      EXPECTED_SHA: 10ad34b8fc2b9d1957f5b467999e738946fb2d6a
      E2E_REGION: Красноярск
      E2E_INDUSTRY: Стоматология
      E2E_EXPECTED_PROVIDERS: searxng,yandex
      E2E_MIN_RETURNED: "10"
      REPORT: /tmp/hunter-real-e2e-stage-${{ github.run_id }}.md

    steps:
      - name: Record explicit owner authorization
        shell: bash
        run: |
          set -Eeuo pipefail
          {
            echo '## Owner-authorized Hunter real website E2E'
            echo
            echo '- authorization: explicit user approval in ChatGPT conversation on 2026-08-09'
            echo '- paid provider calls: authorized for this single Stage run'
            echo "- target SHA: \`$EXPECTED_SHA\`"
            echo "- payload: region=\`$E2E_REGION\`, industries=[\`$E2E_INDUSTRY\`]"
            echo "- expected providers: \`$E2E_EXPECTED_PROVIDERS\`"
          } > "$REPORT"

      - name: Verify exact deployed SHA and healthy auditor
        shell: bash
        run: |
          set -Eeuo pipefail
          cd "$STACK_DIR"
          deployed="$(cat app-source-sha.txt)"
          status="$(docker inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}' "$CONTAINER")"
          [[ "$deployed" == "$EXPECTED_SHA" ]]
          [[ "$status" == healthy ]]
          {
            echo "- exact deployed SHA: \`$deployed\` ✅"
            echo "- stage auditor status: \`$status\` ✅"
          } | tee -a "$REPORT" "$GITHUB_STEP_SUMMARY"

      - name: Execute exact public Hunter route and inspect forensic funnel
        shell: bash
        run: |
          set -Eeuo pipefail
          result="$(docker exec \
            -e E2E_REGION="$E2E_REGION" \
            -e E2E_INDUSTRY="$E2E_INDUSTRY" \
            -e E2E_EXPECTED_PROVIDERS="$E2E_EXPECTED_PROVIDERS" \
            -e E2E_MIN_RETURNED="$E2E_MIN_RETURNED" \
            -i "$CONTAINER" python - <<'PY'
          import json
          import os
          import sqlite3
          import time
          import httpx

          db_path = os.getenv('AIMETON_RUNTIME_DB', 'data/runtime-core.sqlite3')
          db = sqlite3.connect(db_path, timeout=30)
          try:
              before = db.execute('SELECT COALESCE(MAX(rowid), 0) FROM mission_trace_events').fetchone()[0]
          finally:
              db.close()

          payload = {
              'region': os.environ['E2E_REGION'],
              'industries': [os.environ['E2E_INDUSTRY']],
          }
          started = time.monotonic()
          response = httpx.post('http://127.0.0.1:5000/api/hunt', json=payload, timeout=1800.0)
          elapsed = round(time.monotonic() - started, 3)
          response.raise_for_status()
          data = response.json()

          db = sqlite3.connect(db_path, timeout=30)
          db.row_factory = sqlite3.Row
          try:
              row = db.execute(
                  '''
                  SELECT mission_id, attempt_id, counters_json
                  FROM mission_trace_events
                  WHERE rowid > ? AND component = 'hunter' AND operation = 'hunt_funnel_complete'
                  ORDER BY rowid DESC
                  LIMIT 1
                  ''',
                  (before,),
              ).fetchone()
          finally:
              db.close()
          assert row is not None, 'no hunt_funnel_complete event created by public /api/hunt request'
          funnel = json.loads(row['counters_json'])

          candidates = data.get('candidates') or []
          search = data.get('search') or {}
          attempts = search.get('attempts') or []
          called_states = {'succeeded', 'empty', 'failed', 'cache_hit'}
          participating = sorted({
              str(item.get('provider'))
              for item in attempts
              if item.get('provider') and str(item.get('state')) in called_states
          })
          expected = sorted({item.strip() for item in os.environ['E2E_EXPECTED_PROVIDERS'].split(',') if item.strip()})
          missing = sorted(set(expected) - set(participating))

          raw = int(funnel.get('raw_results', 0))
          unique = int(funnel.get('unique_candidates', 0))
          qualified = int(funnel.get('qualified_candidates', 0))
          returned = int(funnel.get('returned_candidates', 0))
          minimum_returned = int(os.environ['E2E_MIN_RETURNED'])

          assert raw >= unique >= qualified >= returned, funnel
          assert returned == len(candidates), (returned, len(candidates))
          assert int(data.get('discovered', -1)) == unique, (data.get('discovered'), unique)
          assert returned >= minimum_returned, (returned, minimum_returned)
          assert not missing, {'missing_expected_providers': missing, 'participating': participating}

          provider_counts = {}
          provider_cost_totals = {}
          for attempt in attempts:
              provider = str(attempt.get('provider') or '')
              state = str(attempt.get('state') or '')
              if not provider:
                  continue
              key = f'{provider}:{state}'
              provider_counts[key] = provider_counts.get(key, 0) + 1
              currency = str(attempt.get('cost_currency') or '')
              try:
                  amount = float(attempt.get('cost_amount') or 0)
              except (TypeError, ValueError):
                  amount = 0.0
              if currency:
                  provider_cost_totals.setdefault(provider, {})
                  provider_cost_totals[provider][currency] = round(provider_cost_totals[provider].get(currency, 0.0) + amount, 6)

          roles = {}
          for item in candidates:
              role = str(item.get('role') or item.get('candidate_role') or 'unspecified')
              roles[role] = roles.get(role, 0) + 1

          safe = {
              'elapsed_seconds': elapsed,
              'region': data.get('region'),
              'query_count': len(data.get('queries') or []),
              'funnel': {
                  'raw': raw,
                  'unique': unique,
                  'qualified': qualified,
                  'returned': returned,
              },
              'candidate_count_in_api': len(candidates),
              'candidate_roles': roles,
              'participating_providers': participating,
              'provider_attempt_states': provider_counts,
              'provider_cost_totals': provider_cost_totals,
              'search_total_cost_by_currency': search.get('total_cost_by_currency') or {},
              'mission_id': row['mission_id'],
              'attempt_id': row['attempt_id'],
          }
          print(json.dumps(safe, ensure_ascii=False, sort_keys=True))
          PY
          )"
          {
            echo
            echo '### Exact public `/api/hunt` result'
            echo
            echo '```json'
            echo "$result"
            echo '```'
            echo
            echo '- verdict: ✅ real user-path Hunter E2E passed.'
          } | tee -a "$REPORT" "$GITHUB_STEP_SUMMARY"

      - name: Upload sanitized evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hunter-real-e2e-stage-${{ github.run_id }}
          path: ${{ env.REPORT }}
          if-no-files-found: warn
          retention-days: 30

      - name: Publish evidence to P1-501
        if: always()
        uses: actions/github-script@v7
        env:
          REPORT_PATH: ${{ env.REPORT }}
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require('fs');
            const path = process.env.REPORT_PATH;
            const body = path && fs.existsSync(path)
              ? fs.readFileSync(path, 'utf8')
              : `## Hunter real website E2E Stage acceptance\n\nWorkflow ${context.runId} failed before sanitized evidence was produced.`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 501,
              body: body.slice(0, 60000),
            });
