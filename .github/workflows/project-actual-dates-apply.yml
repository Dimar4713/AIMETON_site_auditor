name: Project Actual Dates Apply

on:
  issues:
    types: [labeled]
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  apply:
    if: github.event_name == 'workflow_dispatch' || github.event.label.name == 'project:apply-actual-dates'
    runs-on: ubuntu-latest
    environment: project-governance
    env:
      GH_TOKEN: ${{ secrets.PROJECTS_TOKEN }}
      PROJECT_OWNER: ${{ vars.PROJECT_OWNER }}
      PROJECT_NUMBER: ${{ vars.PROJECT_NUMBER }}
      CONFIRM_APPLY: APPLY_91_134
    steps:
      - uses: actions/checkout@v4

      - name: Validate configuration
        shell: bash
        run: |
          set -Eeuo pipefail
          test -n "${GH_TOKEN:-}" || { echo "Missing secret PROJECTS_TOKEN"; exit 1; }
          test -n "${PROJECT_OWNER:-}" || { echo "Missing variable PROJECT_OWNER"; exit 1; }
          test -n "${PROJECT_NUMBER:-}" || { echo "Missing variable PROJECT_NUMBER"; exit 1; }

      - name: Apply approved historical dates
        shell: bash
        run: |
          set -o pipefail
          PYTHONPATH=scripts python scripts/project_actual_dates_apply.py 2>&1 | tee actual-dates-apply-evidence.json

      - name: Upload apply evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: actual-dates-apply-evidence
          path: actual-dates-apply-evidence.json

      - name: Publish evidence
        if: always() && github.event_name == 'issues'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PROJECTS_TOKEN }}
          script: |
            const fs = require('fs');
            const path = 'actual-dates-apply-evidence.json';
            const raw = fs.existsSync(path) ? fs.readFileSync(path, 'utf8').trim() : 'No evidence file was produced.';
            const report = raw.length > 55000 ? `${raw.slice(0, 55000)}\n…truncated` : raw;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: `## Actual dates historical backfill — apply evidence\n\n- workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}\n\n\`\`\`json\n${report}\n\`\`\``
            });
